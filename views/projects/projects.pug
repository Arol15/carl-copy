//- extends ../layout.pug

//- block content
//-   div.wrapper
//-     h4.pb-4.text-muted Team: #{team.teamName}
//-     table.table
//-       thead
//-         tr
//-           th Project ID
//-           th Project Name
//-           th
//-           th
//-       tbody
//-         each project in projects
//-           tr
//-             td= project.id
//-             td= project.projectName
//-             td: .d-flex.justify-content-end
//-               a.btn.btn-secondary.ml-2(href=`/teams/${project.teamId}/projects/${project.id}/columns` role='button') Select
//-               a.btn.btn-secondary.ml-2(href=`/teams/${project.teamId}/projects/${project.id}/edit` role='button') Edit
//-               a.btn.btn-secondary.ml-2(href=`/teams/${project.teamId}/projects/${project.id}/delete` role='button') Delete

//-     div
//-       a.btn.btn-secondary.ml-2(href=`/teams/${team.id}/projects-create` role='button') Add Project
doctype html
include project-utils.pug
head
  meta(charset='UTF-8')
  meta(name='viewport' content='width=device-width, initial-scale=1.0')
  link(rel='stylesheet' href='/stylesheets/home-project.css')
  title SandBox
body
  
  if error
    #myModal.modal(style="display: block")
      include create-modal.pug
  else
    #myModal.modal(style="display: none")
      include create-modal.pug
  .main
    // side-bar__isCollapsed
    .side-bar.side-bar__isCollapsed
      .tabs.home-tasks
        h1 CARL
        div &#x1F3E0; Home
        div &#x2714;  My Tasks
      .tabs.favorites
        h2 Favorites
        p click on the &star; next to a project
      .tabs.projects
        h2 Projects
        .project-list
          span.project-name Asana Clone
          span.favorite-click &star;
        .project-list
          span.project-name spotify Clone
          span.favorite-click &star;
      .tabs.invite
        #button
          p Invite to CARL
    .main-page
      .nav-bar
        .nav-bar-item.side-bar-opener &rAarr;
        h3 HOME
      .project-label
        div YOUR PROJECTS
      .main-page-body
        each existingProject in projects
          .project-square= existingProject.projectName
        .project-square.project-new +
    // <div class='body-page'>dsf</div>
  script(src='/js/index.js')